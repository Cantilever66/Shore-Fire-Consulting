[build]
  base = "shore-fire-github"
  command = "pnpm install --no-frozen-lockfile && pnpm run build"
  publish = "client/public"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "10.4.1"
